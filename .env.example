# Frontend (Vite) — public client id for PayPal (safe to expose)
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id_here

# Server-side (DO NOT commit or expose) — use these on your backend to verify orders / call PayPal APIs
# PAYPAL_CLIENT_ID=your_paypal_client_id_here
# PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here

# Notes:
# - Use Sandbox credentials when developing (developer.paypal.com -> My Apps & Credentials -> Sandbox)
# - Never commit real secrets to source control. Add .env to .gitignore (already present in many projects).
